<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/background_maincolor_rectangle">

    <TextView
        android:id="@+id/notice_title"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center|start"
        android:paddingLeft="15dp"
        android:text="(날짜 영역) 공지 제목 영역"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/notice_line"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#999999"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        app:layout_constraintTop_toBottomOf="@id/notice_title"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/notice_content"
        android:gravity="center|start"
        android:paddingLeft="15dp"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="공지 내용 영역"
        app:layout_constraintTop_toBottomOf="@id/notice_title"
        android:visibility="gone"/>

    <!-- 저작권 표시 (New icon by Icons8) -->
    <ImageView
        android:id="@+id/new_mark"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/new_icon_by_icons8_01"
        android:layout_marginRight="15dp"
        app:layout_constraintBottom_toBottomOf="@id/notice_title"
        app:layout_constraintEnd_toEndOf="@id/notice_title"
        app:layout_constraintTop_toTopOf="@id/notice_title" />

</androidx.constraintlayout.widget.ConstraintLayout>