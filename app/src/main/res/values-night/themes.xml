<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoticeBoard" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
    </style>

    <style name="WhiteStroke" parent="android:Widget.EditText">
        <item name="android:background">@drawable/background_white_rectangle</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
    </style>

    <style name="WhiteRadius" parent="android:Widget.Button">
        <item name="android:background">@drawable/background_white_radius</item>
        <item name="android:textColor">@color/main_color</item>
    </style>

    <style name="MainColorRadius" parent="android:Widget.Button">
        <item name="android:background">@drawable/background_maincolor_radius</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>